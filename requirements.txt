# Upper bounds stop a future major release from silently breaking the build.
# "tested" = the versions this was verified against (Python 3.10 and 3.14).
# For a byte-identical environment, pin to those versions with == instead.
duckdb>=1.0,<2         # tested 1.5.4   writes/reads the .duckdb files
flask>=3.0,<4          # tested 3.1.3   the server
numpy>=1.26,<3         # tested 2.2.6   STFT + tile maths
Pillow>=10.0,<13       # tested 12.2.0  WebP tile encoding
waitress>=3.0,<4       # tested 3.0.2   optional; falls back to Flask if absent
npTDMS>=1.9,<2         # tested 1.11.0  only needed for NI .tdms IQ captures
